# openapi-typescript (devDependency, codegen CLI only) déclare un peer
# typescript ^5.x ; ce projet est pinné sur typescript ~6.0.3 (requis par
# Expo 56). Le conflit est sans risque (pas de TS partagé au runtime).
legacy-peer-deps=true
